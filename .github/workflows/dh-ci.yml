name: DH CI
on:
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: DH intentional failure
        run: |
          echo "simulating a real failing build step"
          exit 1
